{
  "name": "realworld-conduit",
  "version": "1.0.0",
  "description": "RealWorld implementation using Vibe Coding methodology",
  "scripts": {
    "dev": "make dev",
    "build": "make build",
    "test": "make test",
    "lint": "make lint",
    "format": "make fmt",
    "migrate": "make migrate",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "react-router-dom": "^7.6.3"
  }
}
